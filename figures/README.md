# Figure helpers

These modules redraw figures from committed experiment outputs; they do not run
the underlying Monte Carlo simulations. For normal use, invoke the root-level
orchestrator instead:

```bash
python reproduce_paper_figures.py
```

See [`docs/REPRODUCIBILITY.md`](../docs/REPRODUCIBILITY.md) for the figure
manifest and selective-regeneration commands.
